allprojects {
    group 'com.braisgabin.seshat'
    version '0.0.1'

    repositories {
        mavenLocal()
        jcenter()
        maven { url 'https://kotlin.bintray.com/ktor' }
    }
}

// task for Heroku
task stage {
    dependsOn ':api:installDist'
}
